# unfuck-the-project

**In one line:** Ownership prompt, brief template and skills that hand a frontier model an entire project plus questions to escalate and pointers to component and repository banks. District: `Great_Library_of_SISO` (`~/SISO_Workspace/Great_Library_of_SISO/works/unfuck-the-project`).

The rest is in `README.md`. Find anything else on this machine with `estate where <words>`.
