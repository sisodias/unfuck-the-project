# <PROJECT NAME> — dispatch brief

Apply `UNFUCK.md` in full. You are the accountable owner of this project.

## Root and sources

- Project root: `<absolute path or repo URL>`
- Other repos / services that make this project possible: `<paths or URLs, one per line>`
- Original intent lives in: `<transcripts, voice memos, client threads, design docs — exact locations>`
- Client / user feedback lives in: `<locations>`
- Existing entry point (may be stale): `<README / AGENTS.md / CURRENT_STATE path>`

## What we were actually trying to achieve

<Two to five sentences in the human's own words if possible. What outcome matters. Who it is for.>

## Where it hurts (be honest)

<What is broken, confusing, duplicated, stale or slow. What a previous agent did that you doubt. What you keep re-explaining.>

## Current owners and boundaries

- Other agents / people active on adjacent work: `<name — what they own>` (or "none")
- Do not touch: `<paths, branches, panes, environments>`
- Coordinate before changing: `<shared paths>`

## Real constraints

- Privacy / publication: `<what must never leave the project, what is client-confidential>`
- Money / external actions: `<what requires explicit approval>`
- Production / irreversible: `<cutovers, migrations, deletes that need a human>`
- Run / test commands as they exist today: `<commands, or "unknown — discover and record them">`

## Success

<What "materially better" looks like for this specific project. One paragraph.>

## Communication

Keep routine progress in `<handoff / current-state file>`. Surface only a genuinely new blocking decision, an authority conflict or an urgent risk to `<person or routing agent>`.
